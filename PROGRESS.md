# 百忧解开发进度

更新时间：2026-09-05，建立三人并行开发与 AI Agent 集成基线。

## 三人协作基线

- 页面已按职责拆分为 `features/home`、`features/chat`、`features/settings`、`features/schedule` 和 `avatar`，不再由三个人同时修改一个大型页面文件。
- 虚拟形象使用稳定的 `AvatarState`、`AvatarSignal`、`AvatarController` 契约，美术实现可独立替换。
- `docs/OWNERSHIP.md` 固定三人目录所有权；公共模型、数据库迁移、导航、主题、依赖和 Manifest 由集成 Agent 统一修改。
- `docs/TEAM_WORKFLOW.md`、交接单模板、Pull Request 模板和自动构建检查已经建立。
- `tools/team-check.ps1` 会阻止功能分支越界，并拦截 Key、签名材料、APK、数据库和日志进入仓库。
- 固定分支为 `integration`、`feature/chat-and-app-shell`、`feature/calendar-and-message`、`feature/avatar-and-art`；功能只进入 `integration`，验证后再进入 `main`。

## 当前成果

- 0.3.0-dev 将底部导航统一为“主页 / 陪伴 / 对话 / 日程”四个枚举标签，移除旧索引导航；消息分类仍从主页“消息整理”进入。
- 新增陪伴页实时时钟、三套可持久化场景、形象选择框架和三类程序合成环境声；切走页面或退到后台会停止声音。
- 番茄钟支持 25/45 分钟、任务名、三套背景、环境声、本地音频导入、结束提示；按绝对结束时间持久化，返回前台或进程重建后按实际经过时间恢复。
- 对话页双方均有 40dp 圆形头像；用户可通过系统图片选择器设置头像，图片在本机中心裁剪压缩，AI 头像仍为不可编辑占位。
- 当前源码位于工作区 work/baiyoujie-next，未覆盖 Downloads 原文件。
- APP 名称保持百忧解；开发包 cn.edu.ruc.zhixing.dev 与原版隔离，不清除原版数据。
- 三个主页面、本机对话记录、手动待办/日程、记忆查看、模型设置和番茄钟占位页已接通。
- 模型工具结果经过白名单和结构校验，待办/日程只生成建议卡片；确认才写入，支持改标题、忽略、冲突提示和状态恢复。
- 确认操作使用数据库事务，重复点击或并发确认不会重复创建记录；无效事件回滚，不错误标记为已添加。
- 对话后异步抽取记忆及建议，重要消息进入抽取流程；有云端总开关和有限重试。真实服务仍待验证。
- 更换模型服务地址会撤销原云端同意；网络发送前及接收中检查配置是否变化，关闭重定向并限制 SSE 结果大小。
- 课表支持自定义第一教学周周一日期、教学周数、上下课节次、课程、教室、教学周范围及单双周，生成整学期安排。
- 课表修改不会清除手动日程；重复保存不会叠加课程实例。RRULE 元数据保存在模板中，日程表存展开实例。
- 数据库版本 5，提供非破坏性升级路径。
- 保留原自适应分类和人工反馈模块。
- 本版本不发送系统通知；番茄钟在后台按绝对时间继续计算，但不会在后台播放声音或显示通知。AstrBot 未部署。

## 验证证据

- 0.3.0-dev 安装包：`outputs/Baiyoujie-0.3.0-dev-20260905.apk`，26,873,775 字节；SHA-256：`2A4F2C079211FE202F0C331F3FECB4751EE16B8D133962AA82BFC9CD6EE48CA9`。
- 四标签按多种顺序连续点击 20 次后可立即返回主页；全工程运行时源码只保留一处 `NavigationBar`，页面分支为枚举穷举。
- 模拟器无损覆盖安装后完成设备走查：场景在强制停止后恢复；最后标签在强制停止后恢复；系统相册与本地音频文件选择器均能打开。
- 番茄钟后台验证从 `24:57` 恢复为 `24:50`；强制停止再打开恢复为 `24:52`，说明按绝对结束时间恢复。测试完成后已结束测试计时。
- 环境声启动后状态高亮，切到其他标签再返回后自动停止且进程无崩溃。APK 权限仅含网络及 AndroidX 内部动态接收器权限，不含存储或通知发送权限。
- 完整构建、原有单元测试及 3 项新增 UI/计时契约测试通过；构建记录为 `work/baiyoujie-ui-update-build.log`。
- 本轮安装包：outputs/Baiyoujie-0.2.0-dev-20260904.apk，26,536,774 字节。
- SHA-256：839A85F715DAD4138F6BD5D7672C06374BC6AA8E21F21834434F2D7062DAAB9D。
- 最后配色构建成功，随后在该 APK 上再次运行 6 项设备测试通过。

- 37 项本地单元测试通过：AI 规则 6、记忆过期 1、工具抽取 20、课程生成 10。
- 6 项 Android 设备测试通过：版本 1/2/3/4 升级到 5 保留数据、并发确认幂等、忽略后不可确认、无效事件回滚、课表再生成保留手动安排、数据库重开恢复对话和建议。
- Gradle 的设备测试启动层 UTP 未能返回结果，改为直接使用同一 AndroidJUnitRunner 在模拟器上运行；不是把启动器失败视为通过。
- 实际 UI 操作验证：添加待办、设置节次、录入课程、确认生成 4 次课程、建议卡片确认后显示在待办页、强制停止再打开后数据保留。
- 在 1080×2400 和 840×1540 两种尺寸检查了页面，后者约为 320dp 宽；课表编辑器可滚动且未发现文字重叠。测试后恢复模拟器尺寸。
- UI 卡片使用显式标记的本地测试数据，不是真实模型结果。未读取或操作微信聊天。
- APK 权限检查未发现 POST_NOTIFICATIONS。
- 完整逻辑构建日志：work/baiyoujie-final-build.log；最后配色构建：work/baiyoujie-delivery-build.log。
- Android 通过记录：work/baiyoujie-instrumentation-final.log，末尾应为 OK (6 tests)。
- 截图位于 outputs/baiyoujie-*.png；较早截图的弹窗颜色可能早于最后配色调整。

## 尚未完成

1. 系统日历权限、导入/同步及写入；日历月视图和完整日视图。
2. APP 内的数据导出和一键清除。项目 ZIP 备份不等于 APP 用户数据导出。
3. 仅应用内展示的主动陪伴规则、每日限额和去重。
4. 每日记忆整理任务、待办完成关联记忆、近期摘要和上下文完善。
5. 更充分的人设样例、模型情绪标签与形象联动、连续安抚状态跨重启恢复及交互细节。
6. 使用用户自行填写的 Key 验证真实流式回复和真实消息抽取；当前不能宣称全链路 AI 已验收。
7. 真机权限/后台适配、长时间稳定性和完整的原需求回归。

## 下次继续

- 先读本文件及 DECISIONS.md，优先补系统日历与数据管理，再完善主动陪伴和记忆。
- 产品判断不明确时询问用户；用户已批准自定义学期和节次方案，无需重复询问。
- 不向用户索取聊天中的 API Key，不上传真实聊天做测试，不改动原版 cn.edu.ruc.zhixing 数据。
- 测试开发版中的 QA-Course1 是人工录入的测试课程，不是用户真实课表。
- 本地测试命令与工具链参考历史进度文件；设备测试可用 adb shell am instrument -w -r -e class cn.edu.ruc.zhixing.PersistenceTest cn.edu.ruc.zhixing.dev.test/androidx.test.runner.AndroidJUnitRunner。

当前是有实际功能、已验证的开发版本，不是已完成所有需求的最终产品。
